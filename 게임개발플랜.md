# 드림 가디언즈 게임 개발 플랜

## 1. 개발 목표

`AI융 기말과제 게임화 기획.pdf`의 핵심 콘셉트인 `<드림 가디언즈> 위기에 빠진 드림월드를 구해줘!`를 현재 Expo 앱 안에서 구현 가능한 범위로 게임화한다.

목표는 검사를 끝낸 사용자가 자신의 진로 유형을 바탕으로 마을을 구하고, 3단계 미션을 완료하며, 포인트를 얻고, 드림 스토어에서 보상을 확인하는 흐름을 만드는 것이다.

## 2. 현재 앱 기준 전제 

- 현재 검사지 유형은 `탐구형`, `예술형`, `사회형` 3개다.
- 게임 지도에는 이미 5개 마을 화면 구조가 있다.
- 로그인, 검사 결과 저장, 포인트, 진로 미션 인증, 포트폴리오, 부모 리포트, 드림 스토어의 기초 구조가 있다.
- 서버가 없으므로 사진 업로드, 친구 방문, 실제 기부, 오프라인 교환권은 1차 구현에서는 시뮬레이션으로 처리한다.

## 3. 구현 범위

### 1차 구현: 발표 가능한 완성본

1. 게임 홈 리디자인
   - 앱 메인 화면을 게임 홈처럼 보이게 정리한다.
   - 제목은 `드림 가디언즈`로 표시한다.
   - 스토리 문구를 추가한다.
     - `평화롭던 진로의 세계 드림월드의 에너지가 시들어 가고 있어!`
     - `성향 테스트로 발견한 너의 속성으로 마을을 구해줘!`
   - 버튼명 정리:
     - `드림월드` -> `드림월드 입장`
     - `퍼즐 미션 시작하기`, `진로 미션 인증하기` -> `진로 미션 시작하기`로 통합
     - `포인트 사용하기` -> `드림 스토어`

2. 검사 결과 연동 마을 진입
   - 검사 결과가 탐구형이면 `탐구마을`, 예술형이면 `예술마을`, 사회형이면 `소통마을`로 우선 안내한다.
   - 지도에는 5개 마을을 유지한다.
   - 현재 검사지에 없는 `돌봄마을`, `현장마을`은 보너스 마을로 둔다.
   - 안내 문구:
     - `OO마을의 가디언이 되어줘!`
     - `3개의 미션 에너지를 채우면 마을을 구할 수 있어!`

3. 마을 이동 연출
   - 복잡한 애니메이션 대신 단계 화면으로 구현한다.
   - 흐름:
     - 드림월드 지도 선택
     - `OO마을로 이동 중...`
     - `당신의 꿈을 위해 OO마을에서의 여정을 시작합니다!`
     - 미션 3단계 화면 표시
   - 별도 이미지 제작이 어렵다면 현재 `마을_이미지.png`를 배경으로 쓰고 반투명 패널을 올린다.

4. 3단계 미션 시스템
   - 마을마다 `Step 1`, `Step 2`, `Step 3` 미션을 둔다.
   - 처음에는 Step 1만 활성화한다.
   - Step 1 완료 후 Step 2 활성화, Step 2 완료 후 Step 3 활성화.
   - 잠긴 단계는 연한 색으로 표시하고 클릭 불가 처리한다.
   - 각 Step 완료 보상:
     - 자기 결과 유형 마을: `300P`
     - 다른 유형/보너스 마을: `200P`
   - 완료 시 에너지 충전 메시지:
     - `미션 에너지 1/3 충전 완료! +300P`

5. 마을 구출 완료 화면
   - 3단계 완료 시 완료 화면을 띄운다.
   - 문구:
     - `OO마을 구출 완료!`
     - `드림월드를 위한 활동을 더 시작해보세요!`
   - 버튼:
     - `보상 확인하기` -> 드림 스토어로 이동
     - `보너스 진로 퀘스트 하러가기` -> 진로 미션 목록으로 이동

6. 인증 방식 변경
   - 위치 인증 중심은 줄인다.
   - 1차 구현에서는 실제 파일 업로드 대신 아래 입력으로 대체한다.
     - 사진 파일명 또는 사진 설명
     - 한 줄 소감
   - 게시판 형태의 `다른 가디언들의 활동 기록` 영역을 만든다.
   - 댓글 기능은 넣지 않는다.

7. 드림 스토어
   - 홈 화면과 보상 화면에 `드림 스토어` 버튼을 둔다.
   - 상점은 3개 탭으로 나눈다.
     - `내 건물 상점`
     - `리얼 히어로 상점`
     - `리얼 기프트 상점`
   - 1차 구현은 실제 구매/기부/교환이 아니라 포인트 차감과 완료 카드 표시로 구현한다.

8. 드림 가디언즈 인증서
   - 별도 PDF 생성은 1차 범위에서 제외한다.
   - 대신 현재 진로 포트폴리오 화면에 `드림 가디언즈 인증서 출력하기` 버튼을 추가하고 `window.print()`로 인쇄 가능한 화면을 제공한다.

9. 출발 전 체크리스트
   - 진로 미션 시작 전에 체크리스트 팝업 또는 패널을 보여준다.
   - 예시:
     - 운영 시간 확인하기
     - 휴관일 확인하기
     - 보호자와 이동 방법 정하기
     - 사진 인증에 필요한 준비물 확인하기

## 4. 1차 구현에서 제외하거나 시뮬레이션할 기능

- 실제 사진 파일 업로드 및 이미지 저장: 서버/스토리지 필요, 1차는 파일명/설명 입력으로 대체
- 친구 추가와 친구 구역 방문: 사용자 간 데이터 공유 서버 필요, 1차 제외
- 실제 기부 연동: 외부 기관/결제/정산 연동 필요, 1차는 감사 카드 팝업으로 시뮬레이션
- 실제 오프라인 교환권 발급: QR/기관 운영 프로세스 필요, 1차는 교환권 코드 텍스트 생성으로 대체
- 유료 PDF 결제: 결제 모듈 필요, 1차 제외
- 미션형 광고: 실제 광고주/검수 체계 필요, 1차는 샘플 미션 1개만 발표용으로 구현 가능

## 5. 화면 구조

### A. 메인 화면

- 상단: `꿈길 찾기`
- 큰 게임 진입 영역: `드림 가디언즈`
- 주요 버튼:
  - `검사 시작하기` 또는 `검사 다시하기`
  - `드림월드 입장`
  - `여정 현황 확인하기`
  - `드림 스토어`
  - `부모 전용 리포트`
  - `진로 포트폴리오`

### B. 드림월드 홈

- 스토리 카드:
  - 드림월드 위기 설명
  - 내 유형 기반 가디언 안내
- 현재 상태:
  - 레벨
  - 총 포인트
  - 구출 완료 마을 수
  - 미션 에너지 총합
- 버튼:
  - `내 마을로 이동`
  - `전체 지도 보기`
  - `드림 스토어`

### C. 드림월드 지도

- `마을_이미지.png`를 배경으로 사용
- 5개 마을 라벨:
  - 탐구마을
  - 예술마을
  - 소통마을
  - 돌봄마을
  - 현장마을
- 가운데 집은 `홈`
- 마을 클릭 시 마을 내부/미션 화면으로 이동

### D. 마을 미션 화면

- 상단: `OO마을`
- NPC 안내 문구:
  - `OO마을의 가디언이 되어줘!`
- 에너지 상태:
  - `미션 에너지 0/3`
- Step 카드 3개:
  - Step 1 활성
  - Step 2 잠김
  - Step 3 잠김
- 각 Step 내부:
  - 미션 설명
  - 출발 전 체크리스트
  - 인증 입력
  - 완료 버튼

### E. 마을 구출 완료 화면

- 완료 문구
- 획득 포인트
- 다음 행동 버튼:
  - `보상 확인하기`
  - `보너스 진로 퀘스트 하러가기`

### F. 드림 스토어

- 탭 1: 내 건물 상점
  - 성
  - 주택
  - 레스토랑
  - 농장
  - 가축
  - 음료 양조장
- 탭 2: 리얼 히어로 상점
  - 유기동물 보호소 사료 10g 기부
  - 우리 지역 꿀벌 살리기 후원
  - 우리 지역 나무심기 후원
  - 완료 시 감사 카드 팝업
- 탭 3: 리얼 기프트 상점
  - 기관 굿즈 교환권
  - 체험 스티커 교환권
  - 드림 가디언 배지 교환권

## 6. 데이터 구조 변경안

### GameMission

```ts
type GameMission = {
  id: string;
  villageId: GameVillageId;
  step: 1 | 2 | 3;
  title: string;
  prompt: string;
  checklist: string[];
  rewardOwnType: 300;
  rewardOtherType: 200;
};
```

### MissionSubmission

```ts
type MissionSubmission = {
  missionId: string;
  photoText: string;
  reflection: string;
  earnedPoints: number;
  completedAt: string;
};
```

### StoreItem

```ts
type StoreItem = {
  id: string;
  category: 'building' | 'hero' | 'gift';
  title: string;
  description: string;
  cost: number;
  confirmationText: string;
};
```

## 7. 추천 미션 예시

### 탐구마을

- Step 1: 궁금한 현상 하나를 관찰하고 사진 설명 남기기
- Step 2: 원인을 추측하고 자료 1개 찾아보기
- Step 3: 작은 실험 또는 코딩 결과를 기록하기

### 예술마을

- Step 1: 나만의 캐릭터 또는 그림 스케치하기
- Step 2: 작품 제목과 의도 적기
- Step 3: 친구나 가족에게 작품을 소개하고 느낀 점 남기기

### 소통마을

- Step 1: 친구나 가족의 고민을 끝까지 들어보기
- Step 2: 공감 문장 2개 적기
- Step 3: 도움을 준 경험과 느낀 점 남기기

### 돌봄마을

- Step 1: 건강 습관 하나 정하기
- Step 2: 손 씻기, 수면, 마음 건강 중 하나 실천하기
- Step 3: 캠페인 문구 만들기

### 현장마을

- Step 1: 학교나 집의 안전 표지 찾기
- Step 2: 비상구 또는 소화기 위치 확인하기
- Step 3: 안전 수칙 카드 만들기

## 8. 개발 순서

### 1단계: 명칭과 화면 흐름 정리

- 기존 `드림월드` 문구를 `드림월드` 또는 `드림 가디언즈` 중심으로 정리
- `퍼즐 미션` 문구를 `여정 현황` 또는 `진로 미션`으로 변경
- `진로 미션 시작하기` 버튼 통합

### 2단계: 마을별 3단계 미션 진행 구조 구현

- `completedGameMissions`를 step 기반으로 정리
- Step 잠금/해제 UI 구현
- 완료 시 300P/200P 지급
- 에너지 1/3, 2/3, 3/3 표시

### 3단계: 인증 입력과 활동 기록 게시판 구현

- 사진 파일명/설명 입력
- 한 줄 소감 입력
- 완료된 활동 기록 리스트 표시
- 댓글 없음

### 4단계: 마을 완료와 보상 연결

- 3 Step 완료 시 구출 완료 화면 표시
- `보상 확인하기`, `보너스 진로 퀘스트 하러가기` 버튼 연결

### 5단계: 드림 스토어 개편

- 기존 포인트 상점을 3개 탭으로 개편
- 포인트 차감
- 감사 카드/교환권 코드 팝업

### 6단계: 체크리스트와 인증서

- 미션 시작 전 체크리스트 표시
- 포트폴리오/인증서 출력 화면 추가

## 9. 우선순위

가장 먼저 구현할 항목:

1. `진로 미션 시작하기` 버튼 통합
2. 결과 유형 기반 마을 진입
3. 마을별 Step 1/2/3 잠금 해제
4. Step 완료 시 300P/200P 지급
5. 마을 구출 완료 화면
6. 드림 스토어 3탭 구조

발표 완성도를 높이는 항목:

1. 드림월드 스토리 문구
2. 에너지 충전 시각 효과
3. 감사 카드 팝업
4. 출발 전 체크리스트
5. 인증서 출력 버튼

## 10. 최종 발표용 설명 문장

`드림 가디언즈`는 단순한 진로 검사 결과 확인을 넘어, 아이가 자신의 성향을 바탕으로 드림월드의 마을을 구하는 게임형 진로 탐색 서비스입니다. 사용자는 검사 결과에 따라 가디언 속성을 얻고, 3단계 진로 미션을 수행하며, 활동 인증과 소감을 통해 포인트를 얻습니다. 얻은 포인트는 드림 스토어에서 건물 꾸미기, 사회공헌형 보상, 기관 굿즈 교환권으로 사용할 수 있어 진로 탐색이 실제 행동과 보상 경험으로 이어지도록 설계했습니다.
